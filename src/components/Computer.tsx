import { FC } from "react";

export const Computer: FC = () => {
  // const [st, dispatch] = useContext(Context);

  // const remove = (currentIndex: number) => {
  //   dispatch({ type: "removeTodo", id: currentIndex });
  // };

  return (
    <></>
  );
};